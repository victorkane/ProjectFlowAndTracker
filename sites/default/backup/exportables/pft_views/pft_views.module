/**
 * Implementation of hook_views_api().
 */
function pft_views_views_api() {
  return array(
    'api' => 2,
    'path' => drupal_get_path('module', 'pft_views'),
    //'path' => drupal_get_path('module', 'pft_views') . '/includes',
  );
}
